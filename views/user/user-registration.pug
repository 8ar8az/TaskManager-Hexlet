extends /main.pug

include ../forms/user-data-form.pug

block content
  h2.text-center.mt-4= pageTitle
  +userForm('post', getNamedURL('allUsers'))
    .row
      .col-12.col-md-9.col-lg-10.ml-auto
        button.btn.btn-primary.btn-block(type='submit') Зарегистрироваться
