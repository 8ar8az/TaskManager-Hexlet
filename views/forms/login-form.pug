- const borderColor = errors ? 'rgba(255, 0, 0, 0.2)' : 'rgba(200, 200, 200, 0.5)'
form.mt-3.border.rounded.p-3.col-sm-7.col-md-6.col-lg-5.col-xl-4.mx-auto(method='post', action=getNamedURL('session'), style=`box-shadow: 0px 0px 3px 1px ${borderColor};`)
  if errors
    .alert.alert-danger.text-center(role='alert')= _.first(errors).message
  .form-group
    label(for='email') Email:
    input#email.form-control(type='email', name='email', placeholder='Введите ваш email')
  .form-group
    label(for='password') Пароль:
    input#password.form-control(type='password', name='password', placeholder='Введите пароль')
  button.btn.btn-primary.d-block.mx-auto(type='submit') Войти